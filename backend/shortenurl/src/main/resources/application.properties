spring.application.name=shortenurl

# Configuración para producción
server.port=${PORT:8080}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:production}

# Configuración de CORS para producción
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://tu-dominio.pages.dev}

# URL base para URLs cortas
app.base-url=${APP_BASE_URL:https://tu-backend.onrender.com}
